{
  "version": 2,
  "buildCommand": "vite build && esbuild server.ts --platform=node --format=esm --outfile=api/index.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
